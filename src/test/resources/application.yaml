spring:
  datasource:
    url: jdbc:mysql://localhost/checkpoint_test?serverTime=UTC
    username: root
    password: password
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.MySQL8Dialect
