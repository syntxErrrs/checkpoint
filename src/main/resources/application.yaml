spring:
  datasource:
    url: jdbc:mysql://localhost/checkpoint?serverTime=UTC
    username: root
    password: password
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
